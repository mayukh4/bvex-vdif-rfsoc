# Python deps for analyze_vdif.ipynb and the DiFX-prep scripts.
#   python3 -m venv venv && source venv/bin/activate
#   pip install -r requirements.txt
# (numpy-2 compatible wheels; keep numpy/pandas/astropy/baseband consistent.)
numpy>=2.2
scipy>=1.11
pandas>=2.2.3
matplotlib>=3.8
astropy>=6.1
baseband>=4.1.3
jupyterlab>=4.0
